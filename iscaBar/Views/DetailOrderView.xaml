<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="iscaBar.Views.DetailOrderView">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                        <RowDefinition Height="35"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Text="Id:"></Label>
                    <Entry Grid.Row="0" Grid.Column="1" x:Name="xid" Text="{Binding Order.Id}"></Entry>
                    <Label Grid.Row="1" Grid.Column="0" Text="Num:"></Label>
                    <Entry Grid.Row="1" Grid.Column="1" x:Name="xnum" Text="{Binding Order.Num}"></Entry>
                    <Label Grid.Row="2" Grid.Column="0" Text="Diners:"></Label>
                    <Entry Grid.Row="2" Grid.Column="1" x:Name="xdiners" Text="{Binding Order.Diners}"></Entry>
                    <Label Grid.Row="3" Grid.Column="0" Text="Waiter:"></Label>
                    <Entry Grid.Row="3" Grid.Column="1" x:Name="xwaiter"  Text="{Binding Order.Waiter}"></Entry>
                    <Label Grid.Row="4" Grid.Column="0" Text="Client:"></Label>
                    <Entry Grid.Row="4" Grid.Column="1" x:Name="xclient"  Text="{Binding Order.Client}"></Entry>
                    <Label Grid.Row="5" Grid.Column="0" Text="Total:"></Label>
                    <Entry Grid.Row="5" Grid.Column="1" Text="{Binding Order.Total}"></Entry>
                    <Label Grid.Row="6" Grid.Column="0" Text="State:"></Label>
                    <Label Grid.Row="6" Grid.Column="1" Text="{Binding Order.State}"></Label>
                    <StackLayout Grid.Row="7" Grid.Column="0" >
                        <Label Text="Lines: "></Label>
                        <ListView x:Name="MyListView"
                        ItemsSource="{Binding Lineorders}"
                        ItemTapped="Handle_ItemTapped"
                        CachingStrategy="RecycleElement">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"></RowDefinition>
                                                <RowDefinition Height="*"></RowDefinition>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                                <ColumnDefinition Width="*"></ColumnDefinition>
                                            </Grid.ColumnDefinitions>
                                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Product.Name}" Style="{DynamicResource ListItemTextStyle}" />
                                            <Label Grid.Row="0" Grid.Column="1" Text="{Binding Quantity}" Style="{DynamicResource ListItemTextStyle}" />
                                            <Label Grid.Row="0" Grid.Column="2" Text="{Binding Price}" Style="{DynamicResource ListItemTextStyle}" />
                                            <Label Grid.Row="0" Grid.Column="3" Text="{Binding Observations}" Style="{DynamicResource ListItemTextStyle}" />
                                        </Grid>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </Grid>
            </StackLayout>

            <StackLayout Orientation="Horizontal" >
                <Button Text="Modificar" Clicked="ClickedModificar" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Esborra" Clicked="ClickedEsborra" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>